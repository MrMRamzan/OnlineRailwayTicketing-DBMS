@model IEnumerable<OnlineRailwayReservationSystem.Models.Seat>

@{
    Layout = "~/Views/Shared/_DashLayout.cshtml";
}

@{
    ViewBag.Title = "Manage Seats";
}


<div id="page-wrapper">
    <div class="container-fluid">
        <div class="row bg-title">
            <div class="col-lg-3 col-md-4 col-sm-4 col-xs-12">

                <h4 class="page-title">@ViewBag.Title</h4>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- ============================================================== -->
        <!-- table -->
        <!-- ============================================================== -->
        <div class="row">
            <div class="col-md-12 col-lg-12 col-sm-12">
                <div class="white-box">
                    <span class="btn btn-default">@Html.ActionLink("Add New Seat", "Create")</span>
                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead>
                                <tr>
                                    <th>TOTAL SEATS</th>
                                    <th>TOTAL BERTHS</th>
                                    <th>CLASS</th>
                                    <th>TRAIN NAME</th>
                                    <th>ACTION</th>
                                </tr>
                            </thead>
                            <tbody>

                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>@Html.DisplayFor(modelItem => item.SeatNumber)</td>
                                        <td class="txt-oflo">@Html.DisplayFor(modelItem => item.BirthNumber)</td>
                                        <td>@Html.DisplayFor(modelItem => item.Coach.Class)</td>
                                        <td class="txt-oflo">@Html.DisplayFor(modelItem => item.Train.TrainName)</td>
                                        <td>
                                            <span class="btn btn-default">@Html.ActionLink("Update", "Edit", new { /* id=item.PrimaryKey */ })</span>
                                            <span class="btn btn-danger">@Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })</span>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- /.container-fluid -->
    <footer class="footer text-center"> 2018 &copy; E-Ticket </footer>
</div>
